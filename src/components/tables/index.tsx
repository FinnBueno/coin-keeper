import type { FC } from "react";
import { Flex } from "../general/Flex";

export const FinancialTables: FC = () => {
  return (
    <Flex maxWidth="lg" width="100%" bgcolor="red" position="relative">
      Pannenkoek
    </Flex>
  );
};
